services:
  - type: web
    name: smart-alarm-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --host=0.0.0.0 --port=5000 app:app
    plan: free
```

**`backend/requirements.txt`** — add `gunicorn` to it:
```
flask
flask-cors
ultralytics
opencv-python-headless
numpy
waitress

